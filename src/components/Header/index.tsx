import React from 'react';
import { Container } from './styles';

const Header: React.FC = () => {

  return (
    <Container>
      Boilerplate criado por Eduardo M S Mascarenhas (Com Color Switcher)
    </Container>
  );
};

export default Header;
